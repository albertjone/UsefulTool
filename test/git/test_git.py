from git_tool.git_lib import git_clone_repo_from
import os
import shutil



def test_git_clone():
    # repo_url = 'git@gitlab.sh.99cloud.net:openstack/octavia.git'
    # dest = './repos/octavia'
    # git_clone_repo_from(repo_url, dest)
    # shutil.rmtree(dest)
    assert True